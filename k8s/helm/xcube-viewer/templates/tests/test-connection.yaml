apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xcube-viewver.fullname" . }}-test-connection"
  labels:
    {{- include "xcube-viewver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xcube-viewver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
