apiVersion: v1
kind: Service
metadata:
  name: {{ include "xcube-viewver.fullname" . }}
  labels:
    {{- include "xcube-viewver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xcube-viewver.selectorLabels" . | nindent 4 }}
