kind: Service
apiVersion: v1
metadata:
  name: xcube-viewer
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: xcube-viewer
  ports:
    - protocol: "TCP"
      name: http
      port: 80
      targetPort: 80
  type: ClusterIP
